import { expect } from 'chai';
import { ethers } from 'hardhat';

describe('VyperStorage', () => {
  // it('should store the value 101.', async () => {
  //   const contract = await ethers
  //     .getContractFactory('VyperStorage')
  //     .then((factory) => factory.deploy())
  //     .then((contract) => contract.deployed());
  //   // Set value of 101
  //   await contract.set(101);
  //   // Get stored value
  //   expect(await contract.get()).to.equal(101, 'The value 101 was not stored.');
  // });
});
